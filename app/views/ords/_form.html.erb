<%= form_for(@ord) do |f| %>
  <% if @ord.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ord.errors.count, "error") %> prohibited this ord from being saved:</h2>

      <ul>
      <% @ord.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <th>Svenska</th>
    <%= f.text_field :swedish %>
  </div>
  <div class="field">
    <th>Engelska</th>
    <%= f.text_field :english %>
  </div>
  <div class="actions">
    <%= f.submit 'Klar!'%>
  </div>
<% end %>
